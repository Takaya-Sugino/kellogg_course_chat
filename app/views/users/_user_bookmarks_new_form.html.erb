<!-- Form bookmark start -->
<%= form_with(model: @bookmark, local: true) do |f| %>
<!-- Form bookmark end -->

  <!-- Label and input for session_id start -->
  <div class="form-group">
    <%= f.label :session_id %>

    <!-- Input for session_id start -->
    <%= f.collection_select :session_id, Session.all, :id, :to_s, {}, class: "form-control" %>
    <!-- Input for session_id end -->
  </div>
  <!-- Label and input for session_id end -->

  <!-- Label and input for user_id start -->
  <!-- Input for user_id start -->
  <%= f.hidden_field :user_id, value: @user.id %>
  <!-- Input for user_id end -->
  <!-- Label and input for user_id end -->

  <%= f.submit "Create bookmark", class: "btn btn-block btn-outline-secondary" %>
<% end %>
